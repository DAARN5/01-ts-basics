{
  "name": "01-ts-basics",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "gh-pages": "^5.0.0",
    "typescript": "^5.1.6",
    "vite": "^7.1.2"
  },
  "dependencies": {
    "axios": "^1.11.0"
  }
}
